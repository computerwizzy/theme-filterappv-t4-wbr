<div class="container">
  <div class="row ">
    <div class="col-xl-8 col-lg-10 col-md-11  col-12 py-5 m-auto">
      <div class="title content-title mb-4 pt-3">
        <h1 class="text-center title">{{ page.title }}</h1>
      </div>
      <div class="contect">
        {{ page.content }}
      </div>
    </div>
    </div>
   </div>
    <div class="container-fluid">
    <div id="instagram-feed-box"></div>
      </div>

           {% section 'section-custom-html' %}
           {% section 'icon-with-text' %}
           {% section 'product-videos' %}
           {% section 'shop-featured-product' %}
            {% comment %}
           {% section 'index-video-section' %}
            {% endcomment %}